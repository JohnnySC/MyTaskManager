<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/border">

    <TextView
        tools:text="@string/forth_category"
        android:gravity="center"
        android:layout_marginStart="@dimen/small_margin"
        android:layout_marginEnd="@dimen/small_margin"
        android:layout_centerHorizontal="true"
        android:layout_above="@id/task_count_text_view"
        android:layout_marginTop="@dimen/small_margin"
        android:id="@+id/task_category_text_view"
        android:textAppearance="@style/Base.TextAppearance.AppCompat.Headline"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <TextView
        android:id="@+id/task_count_text_view"
        android:layout_width="match_parent"
        android:layout_marginStart="@dimen/small_margin"
        android:layout_marginEnd="@dimen/small_margin"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:gravity="center"
        android:textAppearance="@style/Base.TextAppearance.AppCompat.Subhead"
        android:textColor="@color/colorPrimary"
        tools:text="123" />

    <Button
        android:id="@+id/add_new_task_button"
        style="@style/Base.Widget.AppCompat.Button.Borderless"
        android:text="@string/add"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="@dimen/medium_margin"
        android:layout_below="@id/task_count_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

</RelativeLayout>